pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "CheckIn"
include ':app'
include ':restclient'

include ':paddle_ocr'

project(':restclient').projectDir = new File('../RestClient/app')


project(':paddle_ocr').projectDir = new File('../paddle_ocr/app')
